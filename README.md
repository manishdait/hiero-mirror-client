# hiero-mirror-client

`hiero-mirror-client` is a Java client wrapper designed to easily query the **Hiero Mirror Node REST API**. It integrates seamlessly with the official Hiero SDK classes (like `AccountId`) while providing a clean, builder-style interface to fetch network data.

